name: CI

on:
  push:
    branches:
    - master

jobs:
  build:

    runs-on: windows-2019

    steps:
    - uses: actions/checkout@v2
    - name: Run a one-line script
      run: .\Calicot\Calicot-IaC\Deploy-AzureResourceGroup.ps1
      shell: powershell
